{
  "protocolVersion": "2025-03-26",
  "transport": { "kind": "streamableHttp", "path": "/mcp" },
  "capabilities": { "tools": { "listChanged": true } },
  "tools": [
    { "name": "get_products",
      "description": "Return product-catalog records (fields mirror the CDGC-governed dim_product.csv). Responses are enriched with x-dp-* governed-identity headers by the Contract Metadata Injection policy.",
      "inputSchema": { "type": "object", "properties": { "variant": { "type": "string" } } } }
  ]
}
